{
  "name": "@ownui/dropdown",
  "version": "1.0.0",
  "description": "Dropdown allows users to select from a list of options with semantic theming support.",
  "main": ".src/index.ts",
  "keywords": [
    "dropdown",
    "menu",
    "select",
    "options"
  ],
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src --dts",
    "dev": "pnpm build:fast --watch",
    "typecheck": "tsc --noEmit",
    "build:fast": "tsup src"
  },
  "author": "ferbbo <Diego Benitez>",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=18 || <20.0.0",
    "react-dom": ">=18 <20.0.0"
  },
  "devDependencies": {
    "@storybook/react": "^8.6.12",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tsup": "^8.4.0"
  }
}
